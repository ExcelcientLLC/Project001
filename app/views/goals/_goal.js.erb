$('#goal').html("<p>");
$('#goal').append("</p>");
$('#goal').append("<h1>Editing Goal</h1>");
$('#goal').append("<%= escape_javascript(render 'form') %>");
$('#goal').append("<div class=delete_format><%= escape_javascript(deleteImageButton client_visit_goal_path(@visit.client, @visit, @goal), "Are you sure you want to delete this goal?") %></div>");

