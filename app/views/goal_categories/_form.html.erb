<%= form_for(@goal_category, remote: true, html: { multipart: true }) do |f| %>
  <% if @goal_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal_category.errors.count, "error") %> prohibited this goal_category from being saved:</h2>

      <ul>
      <% @goal_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="edit_label">
      <%= f.label :name %><br />
    </div>
    <div class="edit_field">
      <%= f.text_field :name %>
    </div>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
