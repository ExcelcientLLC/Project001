<%= render 'form' %>

<%= link_to 'Show', client_visit_path(@client, @visit) %>
<%= link_to 'Previous', [:edit, @client, @client.visits.first] %>
<%= link_to 'Next', [:edit, @client, @client.visits.last] %>

<%= render 'goal_list_form' %>

<%= render 'todo_list_form' %>