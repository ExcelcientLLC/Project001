<%= form_for(@to_do, :url => [@visit.client, @visit, @to_do], :remote => true) do |f| %>
  <% if @to_do.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@to_do.errors.count, "error") %> prohibited this to_do from being saved:</h2>

      <ul>
      <% @to_do.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="edit_label">
      <%= f.label :title %><br />
    </div>
    <div class="edit_field">
      <%= f.text_field :title %>
    </div>
  </div>
  
  <div class="field">
    <div class="edit_label">
      <%= f.label :complete %><br />
    </div>
    <div class="edit_field">
      <%= f.check_box :complete %>
    </div>
  </div>

  <div class="large_field">
    <div class="edit_label">
      <%= f.label :description %><br />
    </div>
    <div class="edit_comment_field_2">
      <%= f.text_area :description %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
